---
title: API Documentation - `getStations`
---

## Overview

The `getStations` API is an HTTP service designed to retrieve information about stations. This API does not require any authentication and can be accessed using a simple GET request.

- **Name**: `getStations`
- **Type**: HTTP
- **Sequence**: 2

## Endpoint

- **Method**: GET
- **URL**: `http://localhost:3000/v1/stations/get`
- **Body**: None
- **Authentication**: None

## Headers

When making requests to the `getStations` API, include the following header for authorization purposes:

- `X-Karma-Admin-Auth`: `ajbkbakweiuy387yeuqqwfahdjhsabd`

## Usage Example

To use the `getStations` API, perform a GET request to the provided URL with the specified header:

```
GET /v1/stations/get HTTP/1.1
Host: localhost:3000
X-Karma-Admin-Auth: ajbkbakweiuy387yeuqqwfahdjhsabd
```

Make sure the request includes the `X-Karma-Admin-Auth` header with the appropriate token value to ensure proper access to the API.

## Response

The API will respond with data related to the stations. The response structure and details would depend on the server’s implementation and specific dataset provided by the `getStations` endpoint.

## Conclusion

The `getStations` API provides an easy method to retrieve station data without the need for additional authentication. Ensure the correct header is used in your request for it to be processed successfully.
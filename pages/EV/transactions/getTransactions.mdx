---
title: getTransactions API Documentation
---

# getTransactions API

The `getTransactions` API endpoint is part of our HTTP based service that allows clients to retrieve all transaction records.

## Endpoint Details

- **Sequence:** 1
- **Type:** HTTP

## HTTP GET Request

- **URL:** `http://localhost:3000/v1/transactions/getAll`
- **Method:** GET
- **Authentication:** Bearer Token (required)

### Authentication

To access the `getTransactions` endpoint, you need to provide a valid bearer token in the Authorization header of your request.

- **Bearer Token:** `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwaG9uZSI6Iis5MTk4MTI5NDA3MDYiLCJ1aWQiOiI1Z2VhcGdhcHhzIn0.yNyKfoOQouzSNDwp_5rJ9CAEMVJo5uqueJHNpG-a3Aw`

### Request Body

- **Body:** None

## Usage Example

To make a request to the `getTransactions` endpoint, send an HTTP GET request with the bearer token included in the authorization header.

### Example Request

```http
GET /v1/transactions/getAll HTTP/1.1
Host: localhost:3000
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwaG9uZSI6Iis5MTk4MTI5NDA3MDYiLCJ1aWQiOiI1Z2VhcGdhcHhzIn0.yNyKfoOQouzSNDwp_5rJ9CAEMVJo5uqueJHNpG-a3Aw
```

This request will retrieve all transactions available on the server, provided that the token is valid and has the necessary permissions.

## Notes

- Ensure your token is kept secure and is not shared publicly.
- Verify that the service running at `localhost:3000` is accessible and operational before making requests.
- This endpoint does not require any payload in the request body.
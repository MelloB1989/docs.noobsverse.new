---
title: Update User API
meta:
  name: updateUser
  type: http
  seq: 4
---

import { Tabs, TabItem } from '@theme/Tabs';

# Update User API

The Update User API allows you to modify user details such as age and weight.

## HTTP Request

Make a `POST` request to the following URL:

```
POST http://localhost:3000/v1/user/update
```

## Authentication

This endpoint requires Bearer Token authentication. Use the following token in the `Authorization` header of your request:

```
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJwaG9uZSI6Iis5MTk4MTI5NDA3MDYiLCJ1aWQiOiI1Z2VhcGdhcHhzIn0.yNyKfoOQouzSNDwp_5rJ9CAEMVJo5uqueJHNpG-a3Aw
```

## Request Body

The request body should be in JSON format and include the following fields:

<Tabs>
<TabItem value="JSON" label="JSON" default>

```json
{
  "age": "69",
  "weight": "86"
}
```

</TabItem>
</Tabs>

## Summary

When you send a `POST` request to the `/v1/user/update` endpoint with the required authentication and JSON body, you can update the user's information such as age and weight. Ensure that you include the correct authentication token in the `Authorization` header to successfully authenticate your request.